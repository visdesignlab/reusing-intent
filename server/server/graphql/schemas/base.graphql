schema {
  query: Query
  mutation: Mutation
}

type Query {
  hello: String!
  projects: ProjectsResult!
  data(record_id: ID!): DataResult!
  predictions(record_id: ID!): PredictionsResult!
}

type Mutation {
  createProject(projectName: String!): ProjectResult!
}
